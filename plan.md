# Branding Update: shop with swago

Update the site's branding from "ESSENCE" to "shop with swago" to reflect the new brand identity.

## Changes
1.  **Navbar Update**: Modify `src/components/Navbar.tsx` to display "shop with swago" instead of "ESSENCE.".
2.  **Consistency Check**: Update `src/App.tsx` (Footer section) to match the new branding if applicable, ensuring a unified brand experience across the home page.

## Implementation Details
- Change text in `src/components/Navbar.tsx`.
- Change branding text and copyright in `src/App.tsx`.
